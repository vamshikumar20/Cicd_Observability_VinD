apiVersion: v1
kind: Service
metadata:
  name: {{ include "payment-gateway-service.fullname" . }}
  labels:
    {{- include "payment-gateway-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "payment-gateway-service.selectorLabels" . | nindent 4 }}
